import { ReceitaUpgradeStatements } from './receita.upgrade.statements';

describe('ReceitaUpgradeStatements', () => {
  it('should create an instance', () => {
    expect(new ReceitaUpgradeStatements()).toBeTruthy();
  });
});
